<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Autoriser l'accès aux fichiers dans le cache -->
    <cache-path name="cache_images" path="." />

    <!-- autorise l’accès à tout ce qui est dans filesDir -->
    <files-path name="internal_files" path="." />
</paths>